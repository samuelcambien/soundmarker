import { Circle, Line, Stroke, Polygon, Vertex, Polyline } from 'angular-svg';

export class Drawer {

  // constructor(private document: Document) {
  //
  // }
  //
  // public draw(element_id, buffer: AudioBuffer, width, barWidth, sampleSize) {
  //
  //   let bars = width / barWidth;
  //   let samplesPerBar = buffer.length / bars;
  //
  //   let vols = [];
  //
  //   for (let i = 0; i < bars; i++) {
  //     let start = i * sampleSize;
  //
  //     let vol = 0;
  //
  //     for (let s = 0; s < samplesPerBar; s++) {
  //       let sample = buffer.getChannelData(0)[s];
  //       vol += sample;
  //     }
  //
  //     vols[i] = vol / samplesPerBar;
  //   }
  //
  //   var draw: SVGPathElement = document.createElementNS('http://www.w3.org/2000/svg', 'path');
  //
  //   for (let i = 0; i < bars; i++) {
  //     draw.createSVGPathSegClosePath()
  //   }
  // }
}
