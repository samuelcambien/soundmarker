import {DurationDirective} from './duration.directive';

describe('DurationDirective', () => {
  it('should create an instance', () => {
    const directive = new DurationDirective();
    expect(directive).toBeTruthy();
  });
});
